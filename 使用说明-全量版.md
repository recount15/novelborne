# Novelborne 本地全量版使用说明

> v3.0.0 开发候选版，尚未完成发布验收。本地目录可能含私有恢复资产、历史存档与第三方内容，仅限授权的本机使用，不得直接公开分发。

## 启动

双击 `启动全量版.bat`，需要可用的 Python 环境。该脚本使用端口 **21601**，浏览器地址为 `http://127.0.0.1:21601/`。关闭服务窗口前先等待保存或任务安全停止。

需要仅本机访问时，可在项目目录手动执行：

```bash
python run_app.py --host 127.0.0.1 --port 21601
```

默认运行数据在本目录 `var/`；如显式指定其他运行目录，以实际启动参数为准。不同实例必须使用不同端口与数据目录，勿同时写同一个数据库。

## v3 与旧全量恢复的区别

- 活动角色库只使用数据库，新数据库为空是预期行为。
- 旧 JSON、Markdown、作品库和恢复包不会自动作为角色库回退。历史资产数量不等于当前活动角色数量，不再列出私有作品、角色名单或旧统计。
- 角色设计器使用统一角色库保存，不再要求先存 persona 再另存角色。
- “仅蒸馏”提交全书准备任务；`READY` 代表资料就绪，不代表已玩、已开局或真实模型质量验收通过。
- 原文精确/文本近似搜索不需要模型。阅读访谈以当前章末为界；“从本章开始”以章首为界，确认后创建新局，不覆盖旧局。

完整操作见 [用户手册](docs/USER_MANUAL.md)。功能集成仍在进行，遇到缺失控件或失败状态请保留脱敏错误，不假定保存、恢复或开局已成功。

## 备份与隔离

1. 停止所有使用该数据目录的实例。
2. 将数据库、会话、上传数据与必要配置备份到私有 `<backup-root>/<timestamp>/`，核实文件可读后再操作。
3. 旧资产需隔离时使用私有 `<quarantine-root>/<timestamp>/`，保留来源和恢复依据。示例路径不代表程序已自动迁移。
4. 恢复前先确认目标版本、目录和覆盖范围。**不要把旧 `双击恢复.bat` 或 `--force` 当作日常升级步骤**，它们可能覆盖本地新数据；本次文档/版本更新未执行恢复或修改生产数据。

不要把备份、隔离区、运行数据库、原著、日志或恢复包打进公开发布包。不要上传带 Key、个人路径或会话链接的截图。本文暂未附已核实的运行截图。

## 发行边界

源码版本号为 3.0.0 不意味着目录中的旧 EXE 已升级。便携包必须独立重建与实机验收；未核实的包不用于正式分发。发布状态见 [候选说明](docs/RELEASE_NOTES_v3.0.0.md)。

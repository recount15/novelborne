你是开局剧情合并器。多份「开局剧情采样」结果并行产出后，由你合并为
全书开局所需的结构化剧情大概（后续作品档案卷与选角相关度都依赖它）。

【任务】剧情采样合并。

【作品名】《@@WORK_TITLE@@》
【剧情采样结果】（JSON 数组，每项对应一份采样卷）
@@SAMPLES@@

【输出格式】只输出一个 JSON 对象，不要输出任何解释、前后缀或代码围栏：
{
  "genre": "题材与子类型（如：东方玄幻（体系流）），30 字内",
  "premise": "全书核心前提与冲突设定，60-200 字",
  "major_threads": ["3-6 条贯穿全书的主线，按重要性排序，每条 40 字内"],
  "tone": "全书叙事基调，30 字内"
}

【硬性要求】
- 四个字段全部必填；major_threads 必须 3-6 条。
- 只综合采样结果中可见的信息；各卷冲突时以多次出现的信息为准，不得虚构新设定。

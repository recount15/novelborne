# Repository metadata for v2.0.1

## GitHub / Gitee description

### 中文

大模型驱动的互动小说世界模拟器：结构化剧情丰度、锚点与角色状态、任务/碎锚、并发生成、空级批改和全局润色。

### English

LLM-driven interactive novel world simulator with structured narrative richness, anchors, character state, per-slot grading, concurrent generation, and global prose polishing.

## Suggested topics

```text
interactive-fiction
llm
novel
story-engine
ai-writing
role-playing
fastapi
vue3
openai-compatible
prompt-engineering
python
typescript
```

## Website field

Leave empty unless an HTTPS public deployment is available. Do not use a LAN address, session URL, QR-code URL, or API-provider URL as the repository website.

## Release title

```text
Novelborne v2.0.1 — Structured Narrative Pipeline
```

## Release summary

v2.0.1 restructures Enhanced Mode into blueprint-driven concurrent filling, per-slot grading and refill, deterministic assembly, and non-blocking global prose polishing. It also adds six narrative-richness levels, parallel opening distillation, evidence-backed character state patches, a structured directive ledger, dynamic API concurrency, and repaired LAN/session QR handoff.

## Security / privacy notes

- Never place provider tokens in repository secrets visible to forks or in workflow logs.
- Do not upload `var/`, databases, logs, uploaded TXT manuscripts, real-model test reports, session URLs, QR images, or private recovery packages.
- Release artifacts must be built from the clean public source tree and verified independently before upload.

# v2.0.2 Release Notes

修复版：聚焦原著阅读器、存读档保真与局域网扫码链路。全部修复均经真实浏览器 / 真实服务进程验证后收录。

## 原著阅读器

### 滚动跨章翻页

- 修复"无法到达下文"：下章预览区渲染条件放宽（邻章数据存在即渲染，含空文本占位），邻章加载失败自动重试一次；预览缺失导致的跨章死路彻底消除。
- 修复向上滚动"跳章"（连跳到上一章的上一章）：滚动方向判定移到抑制/加载锁检查之后，程序定位后的基准不再被惯性滚动污染；向上切章稳定抑制从 500ms 提高到 800ms，回上一章锚定章尾而不是被推到边界再次触发。
- 三章滑动窗口：视口底部阅读前沿深入下章预览 60% 才后移窗口，开始读预览不切章；切章采用精确文本锚定（跨章前后阅读的那一行不动），向上切章锚定视口顶部。
- 章内阅读比例保存与恢复使用同一"章内可滚动范围"分母，书签往返零漂移。

### 书签

- 创建、重命名、删除全部改为页面内联 UI（两步删除确认 + 2600ms 自动复位）。嵌入式 webview（IAB / WebView2 宿主）会静默吞掉 `window.prompt/confirm`，原生弹窗路径在窗口版中等于按钮失灵，此为根因修复。
- 书签跳转加加载锁守卫：三连点只放行第一次，不再产生重复章节请求。
- 切章加载锁新增 10 秒看门狗：任何路径卡死时强制复位，书签跳转/翻章/目录点击不会被永久静默拦截。

### 新增面板

- 本章锚点小窗、本章活跃人物小窗：数据来自锚点蒸馏产物（每章锚点结构化摘要与在场人物富文本），新增只读接口 `GET /api/books/{book_id}/chapters/{chapter_index}/anchors`；未蒸馏章节显示优雅空状态。
- 键盘翻页（方向键 / 空格 / PageUp / PageDown）、页内搜索高亮、翻页按钮兜底（小屏同样可用）。

## 存读档

- 修复"读档后无法交互"：读档端点镜像补齐 `gf_confirmed` / `opening_confirmed` 顶层键并兜底 options，确认前与进行中的档读入后界面立即有可用交互入口。
- 回合中段自动存档移到选项定稿之后，不再产生无 options 的自动档覆盖完整档。
- 读档保真：开局蒸馏终态、剧情摘要、蒸馏键与锚点目录原样恢复，读档零重蒸馏（端到端 12 项验证：7 章锚点冻结零变更、读档后真实回合 1→2、二次存读档闭环）。
- 恢复 / 读档后聊天区不再显示世界书拼装 prompt（展示层截断拼装标记，模型上下文不受影响）。

## 局域网与手机扫码

- 手机扫码接续、移动端布局（390px 无横向溢出、抽屉全宽、安全区）、阅读器小屏适配与翻章按钮路径实测通过。
- 窗口版 12 秒退出问题修复：从最终源码重打包并独立启动验证（进程存活、health 2.0.2、局域网信息、二维码 PNG、会话接续、窗口壳）。

## 角色库与开局进度

- 角色卡双写策略明确：JSON 为主存储，数据库写失败降级为告警不阻断；实测 6 张角色卡全部入库、合并池路径一致。
- 开局蒸馏进行中状态轮询补全：合并键未就绪时返回通用 running 阶段，章节完成计数实时可见，前端无数据时显示默认文案。

## 验证

- Python 单元 / 集成测试：351 项全部通过。
- 前端：`vue-tsc -b && vite build` 通过。
- 端到端真实流程（隔离环境 + 真实模型）：开局蒸馏 → 设定确认 → 首幕 → 存档 → 读档 → 读档后真实回合 → 二次存读档，全部通过；锚点 sha256 冻结比对零重蒸馏。
- 阅读器真实浏览器实测：连续下翻 1→2→3→4 顺序零跳章、上翻 4→3 回退锚定、跨章后急速反向；书签创建 / 跳转（比例恢复精确到位）/ 三连点防护 / 重命名 / 两步删除 / 确认窗自动复位全部通过；390px 手机视口复测通过。
- Windows Web / 窗口版均从最终源码重建并独立启动验证。

## 许可证

- 公开源码与二进制发布采用 BSD 3-Clause License。
- 运行数据继续存放于 `var/`，不会包含在公开源码和 Release 压缩包中；本地测试用的版权文稿与密钥不属于任何发布产物。

## 兼容性

- 旧存档、旧书签与阅读进度键继续兼容；普通模式 legacy 生成路径不变。

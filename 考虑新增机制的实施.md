# 考虑新增机制的实施

> 文档性质：后端架构重构设计与实施路线
>
> 适用源码：当前 `Novelborne-2.0.0-clean-source`
>
> 核心目标：在不丢失现有机制的前提下，把后端演进为一个**专用的剧情运行 Agent**。它不是通用问答 Agent，而是围绕原著理解、角色一致性、锚点推进、许愿兑现、时序逻辑、选项生成、状态持久化和长剧情稳定性设计的专门系统。

---

## 一、总体目标与约束

### 1.1 目标

新的后端应当具备以下能力：

1. **比当前单回合生成更强的长剧情推演能力**：能够同时理解已经发生的事实、当前锚点、未来锚点、角色目标、原著约束和玩家长期意图。
2. **充分利用原著材料**：原著不再只是一次性前情文本，而是可检索、可定位、可分层缓存的世界模型来源。
3. **通过代码提供基础逻辑**：时序、因果、状态变化、角色知情范围、能力边界、许愿兑现窗口等不能全部交给模型自由发挥。
4. **保留现有机制**：锚点、碎锚、涟漪、任务、金手指、宿敌、角色卡、角色蒸馏、skill drift、质量门、批改重填、上下文压缩、存档和导出都继续存在，并被 Agent 统一编排。
5. **允许高 token 消耗**：在不牺牲稳定性的前提下，单回合 token 可达到当前的 10 倍甚至更高。
6. **等待时间不能显著增加**：最长章节当前约 5 分钟；新架构应通过并行、缓存、增量更新和局部重填，把端到端等待控制在接近当前水平，目标为普通回合 30–90 秒，复杂回合 2–5 分钟。
7. **支持 Anthropic 与 OpenAI 缓存**：缓存必须是模型供应商无关的抽象层，同时适配两家的 prompt caching 能力。
8. **不把私有 IP 数据放入公开发行包**：架构设计只针对当前源码，私有资源仍遵守本地恢复与公开发布隔离原则。

### 1.2 非目标

- 不把系统改造成通用自主 Agent、浏览器 Agent 或开放式工具调用 Agent。
- 不让模型直接修改 SQLite、存档或核心状态；模型只能提交结构化提案，由代码校验和落地。
- 不用“无限自我反思”替代确定性校验。
- 不通过增加更多串行模型调用来换取质量；高 token 预算必须主要通过并行和缓存实现。

---

## 二、当前源码可复用基础

当前源码已经具备一个“类 Agent”雏形，应在其上收敛，而不是推倒重来。

### 2.1 现有编排基础

- `core/services/turn_pipeline.py`
  - 已有导演卷 → 段卷/选项卷并发 → 批改 → 重填 → 组装的试卷化流程。
  - 是新 Agent 的主要回合执行器候选。
- `core/engine/agent_mode.py`
  - 已有 draft → self-check → revise 结构。
  - 可改为 Agent 内部的质量修订子循环。
- `core/engine/parallel.py`
  - 用于并发执行独立模型任务，避免 token 增长直接转化为等待时间增长。
- `core/engine/context_compressor.py`
  - 已有接手包、摘要、尾部原文和保真度校验。
  - 应扩展为多层长期记忆，而不是只在每 10 回合压缩 history。
- `core/engine/turn_blueprint.py`
  - 已有回合蓝图和锚点计划概念。
  - 新 Agent 应使蓝图成为所有生成阶段共享的唯一剧情计划。
- `core/engine/turn_grader.py`、`quality_gate.py`、`elastic_gate.py`
  - 继续作为硬门禁与局部重填依据。
- `core/engine/directives.py`
  - 继续维护许愿/铁律账本，但要从“提示词注入”升级为结构化剧情目标。
- `core/services/model_gateway.py`、`core/engine/distill.py`
  - 作为所有模型调用和 token 计量的统一入口。
- `core/engine/opening_distill.py`、`character_semantic_distiller.py`
  - 负责开局原著与角色蒸馏，结果应进入长期 Agent 记忆。
- `core/services/context_retrieval_service.py`、`core/lore/*`
  - 作为原著和世界书检索入口。

### 2.2 当前架构的主要瓶颈

1. `on_send()` 仍承担过多职责，回合状态、模型调用、许愿选择、压缩、持久化耦合。
2. 许愿当前主要通过 `select_for_turn()` 进行相关性选择，再拼到 system prompt；没有稳定的“目标 → 证据 → 选项响应 → 兑现检查”链路。
3. 上下文压缩偏向“摘要 + 最近四条”，对长期因果、未完成任务、角色隐性目标和未来锚点的表达还不够结构化。
4. 原著材料有检索能力，但尚未成为可复用、可缓存的世界模型层。
5. 部分模型调用虽可并行，但整体流程仍存在不必要的串行重写和整章重生成。
6. 质量门主要判断输出结果，尚未形成“先规划、再生成、再验证状态迁移”的 Agent 闭环。

---

## 三、专用剧情 Agent 的总体架构

建议采用六层结构：

```text
HTTP / UI
   ↓
Session Runtime
   ↓
Story Agent Orchestrator
   ├─ World Model Builder       原著、世界书、锚点、时间线
   ├─ State & Causality Planner 状态、因果、时序、任务、许愿
   ├─ Character Director        角色思维、决策、语言、边界
   ├─ Narrative Composer        正文、段落、选项
   ├─ Verification & Repair     机械门禁、语义审查、局部重填
   └─ Memory & Cache Manager    长期记忆、检索缓存、供应商缓存
   ↓
Durable State / Save / Export
```

核心对象建议命名为：

```python
class StoryAgent:
    async def run_turn(self, request: TurnRequest) -> TurnResult: ...
```

它只服务于 Novelborne 的剧情回合，不接受任意外部工具，不直接暴露模型 SDK。

### 3.1 Agent 的职责边界

Agent 负责：

- 读取当前状态快照；
- 构建本回合世界模型；
- 推演候选剧情路径；
- 选择并推进当前锚点；
- 把许愿转换为可观察剧情目标；
- 生成正文和 A-F 选项；
- 检查时序、因果、角色一致性和状态变化；
- 对失败部分进行局部修复；
- 返回可落盘的结构化结果。

Agent 不负责：

- 修改用户配置；
- 直接写 SQLite；
- 直接上传文件；
- 自行执行不可逆外部操作；
- 自行改变游戏规则或绕过硬门禁。

---

## 四、单回合 Agent 执行协议

### 4.1 回合不是一次生成，而是一份“事务”

每回合应建立独立的 `TurnTransaction`：

```python
@dataclass
class TurnTransaction:
    turn_id: str
    before_state_hash: str
    snapshot: StorySnapshot
    plan: TurnPlan | None = None
    candidate: TurnCandidate | None = None
    validation: ValidationReport | None = None
    committed: bool = False
```

只有满足以下条件才允许提交：

- `before_round + 1 == after_round`；
- 状态 schema 校验通过；
- 锚点状态迁移合法；
- 角色状态变化符合能力与知情范围；
- 正文和选项满足硬格式；
- 许愿/任务/碎锚有明确推进或明确受阻；
- 不存在未恢复的模型或解析错误；
- 所有 patch 都经过确定性校验。

### 4.2 推荐的流水线

```text
A. Snapshot
   ↓
B. Retrieve / Build World Model       并行检索原著、世界书、长期记忆
   ↓
C. Strategic Rollout                  并行生成 3–6 条短期候选路径
   ↓
D. Causal & Temporal Arbitration      代码筛选 + 模型裁判
   ↓
E. Turn Blueprint                     生成唯一正式施工图
   ↓
F. Parallel Drafting                  段落、选项、角色反应、状态 patch 并行
   ↓
G. Verification                       机械校验 + 语义校验 + 状态模拟
   ↓
H. Local Repair                       只重填失败段/选项/patch
   ↓
I. Commit                             单次提交并写入记忆
```

### 4.3 为什么可以增加 10 倍 token 但不显著变慢

增加 token 不应全部变成一条更长的串行 prompt，而应拆成并行工作：

- 3–6 个候选路径并发生成；
- 原著检索、角色状态分析、时间线检查并发；
- 正文段落与选项卷并发；
- 每个失败槽位独立重填；
- 固定 system/world prompt 使用供应商缓存；
- 已稳定的章节摘要、角色卡和原著片段不重复上传。

目标是“横向增加推演量”，而不是“纵向增加等待链”。

---

## 五、长剧情推演：三层世界模型

### 5.1 静态世界层

来源：

- 原著全文/章节；
- `assets/rules/work_library.md`；
- 世界书条目；
- 开局蒸馏结果；
- 角色卡与角色四维 transfer；
- 能力、代价、关系和知识范围。

特点：低频变化、高复用、适合长期缓存。

### 5.2 叙事时间层

记录：

- 已发生事件；
- 当前锚点；
- 即将发生的锚点；
- 未完成任务；
- 已激活但未兑现的许愿；
- 角色目标与冲突；
- 玩家已经做出的不可逆选择；
- 伏笔、承诺、未结因果。

这一层应由代码维护结构化索引，不只依赖自然语言摘要。

### 5.3 当前场景层

每回合动态生成：

- 当前地点与时间；
- 在场角色及其知情范围；
- 玩家行动；
- 本回合允许推进的事件；
- 本回合禁止提前发生的事件；
- 正文必须留下的证据；
- 选项必须提供的行动接口。

三层模型的组合顺序固定为：

```text
静态世界约束 > 长期叙事事实 > 当前场景计划 > 模型自由发挥
```

---

## 六、代码级时序与因果逻辑

### 6.1 事件图

增加一个纯计算模块，例如：

```text
core/engine/story_graph.py
```

维护：

```python
@dataclass
class StoryEvent:
    event_id: str
    title: str
    round_created: int
    earliest_round: int | None
    latest_round: int | None
    prerequisites: list[str]
    consequences: list[str]
    status: str  # planned/active/resolved/blocked
    evidence_terms: list[str]
```

代码必须检查：

- 前置事件未完成时，不得直接完成后置事件；
- 未来锚点不得无理由提前兑现；
- 已撤销事件不得重新当作未发生；
- 同一事实不能在同回合被互相矛盾地写入；
- 事件发生后，其后果必须进入 state patch 或长期记忆。

### 6.2 状态迁移模拟

模型提交的不是“最终状态”，而是：

```json
{
  "events": [...],
  "state_patch": {...},
  "evidence": [...],
  "option_effects": [...]
}
```

代码先在临时状态上执行 `apply_turn()`，再检查：

- 数值范围；
- 能力冷却；
- 关系变化；
- 物品唯一性；
- 任务生命周期；
- 角色知情边界；
- 宿敌隐藏信息；
- 涟漪/收敛规则。

### 6.3 因果证据

每个计划事件必须至少有：

- 触发原因；
- 可观察动作；
- 结果或受阻结果；
- 对后续事件的影响。

如果正文没有给出这些证据，判定为“叙事描述但未落地”，进入局部重填。

---

## 七、原著材料的使用方式

### 7.1 原著检索不应每回合重新从零处理

建立 `WorldModelCache`：

```python
@dataclass
class WorldModelCache:
    work_id: str
    source_hash: str
    chapter_index: dict
    anchor_index: dict
    character_index: dict
    lore_index: dict
    static_prompt_hash: str
```

首次开局完成：

- 章节切分；
- 角色、地点、组织、能力抽取；
- 事件和锚点索引；
- 章节摘要；
- 关键原文证据窗口。

之后每回合只做增量检索和增量摘要。

### 7.2 检索结果分为三种

1. **硬事实**：必须遵守，来自原文或代码状态。
2. **软参考**：用于风格、语气和背景补足。
3. **禁用推断**：原著没有证据，模型不得自行补全为确定事实。

提示词中必须明确标记三者，避免原著的模糊描述被模型误当成硬设定。

### 7.3 原著证据窗口

对于当前章节和锚点，优先提供：

- 当前锚点前后的原文窗口；
- 当前在场角色首次/关键出场证据；
- 与玩家行动直接相关的原文片段；
- 未来事件只提供“索引和约束”，不提前泄露全部剧情文本。

---

## 八、角色 Agent 与四维蒸馏

角色不应只是一段简介，而应以四维结构进入运行时：

1. `mind_model`：关注点、推理方式、误判模式；
2. `decision_policy`：决策原则、触发器、优先级和冲突取舍；
3. `voice_transfer`：句式、节奏、口头禅、禁用表达、证据台词；
4. `behavior_boundaries`：能力边界、代价/冷却、关系边界和知情范围。

运行时每回合只注入与当前场景相关的摘要，完整角色卡放入缓存上下文，避免每次重复占用输入 token。

角色 Agent 应并行输出：

- 角色对玩家行动的理解；
- 角色本回合目标；
- 角色可采取的动作；
- 角色不能知道的内容；
- 一句或一段符合 voice transfer 的反应建议。

最后由导演 Agent 仲裁，防止多个角色各自生成互相冲突的剧情。

---

## 九、许愿、任务与机制的统一目标层

现有许愿不能只作为 prompt 文本。建议统一为 `NarrativeObjective`：

```json
{
  "objective_id": "wish-12",
  "kind": "wish",
  "fact": "玩家希望获得某能力",
  "scope": "world",
  "activated_round": 4,
  "deadline_round": 7,
  "required_evidence": ["能力名称", "获得动作", "代价或限制"],
  "allowed_progress": ["铺垫", "部分兑现", "明确受阻"],
  "forbidden_outcomes": ["静默跳过", "无代价全能兑现"],
  "option_requirement": "至少一个选项必须能继续推进该目标"
}
```

同样的目标层可承载：

- 金手指；
- 任务；
- 碎锚阶段；
- 宿敌行动；
- 角色长期目标；
- 玩家承诺或不可逆选择。

### 9.1 统一注入位置

当前回合目标必须同时进入：

- 战略推演；
- 正式蓝图；
- 段落生成；
- 选项生成；
- 状态 patch 生成；
- 终局质量检查。

### 9.2 兑现规则

代码检查正文和选项：

- 正文必须有可观察推进、结果或明确阻碍；
- 需要行动的目标，A-F 至少一项必须提供对应行动；
- 关键词不足时不能跳过检查，应由目标层提供 evidence terms；
- 未兑现目标继续留在长期记忆，并自动提高后续回合优先级。

---

## 十、生成与验证的并行策略

### 10.1 推荐并发波次

**Wave 0：缓存命中与检索**

- 读取静态世界模型；
- 读取长期叙事摘要；
- 检索当前锚点原文；
- 检索角色证据。

**Wave 1：候选推演**

并行生成 3–6 个短计划，每个计划只输出结构化事件，不生成长正文。

**Wave 2：仲裁**

代码先过滤明显非法计划，再让一个裁判模型在剩余计划中选择；裁判只处理短 JSON，速度快。

**Wave 3：正式生成**

并行生成：

- 正文段落；
- A-F 选项；
- 角色反应校验；
- 状态 patch 草案。

**Wave 4：局部修复**

- 只修复失败段；
- 只修复不合格选项；
- 只修复冲突 patch；
- 不轻易重写整章。

### 10.2 Keep-best

每个阶段都保留评分最高且满足硬约束的候选：

```text
候选生成 → 机械过滤 → 质量评分 → 语义裁判 → 保留最佳
```

禁止出现“最后一次模型输出覆盖前面更好结果”的行为。

### 10.3 长章节策略

长章节不应一次请求完整正文。建议：

- 先生成 4–8 个段落蓝图；
- 段落并发生成；
- 段落之间通过前一段的结构化 handoff 连接；
- 只把必要的上一段尾部和状态变化传给下一段；
- 最后做一次轻量整合，而不是整章重写。

这样可以扩大推演 token，又不把失败成本扩大到整章。

---

## 十一、缓存架构：Anthropic 与 OpenAI

### 11.1 统一缓存接口

在 `core/services/model_gateway.py` 或独立模块中增加：

```python
class PromptCacheAdapter(Protocol):
    def build_cached_messages(self, layers: PromptLayers) -> Any: ...
    def cache_metadata(self) -> dict[str, Any]: ...
```

统一分层：

```text
L0：供应商 system cache       最稳定
L1：作品世界模型 cache         按 work/source_hash
L2：章节与角色 cache            按 chapter/character/version
L3：本回合动态上下文            不缓存或短 TTL
```

### 11.2 Anthropic

使用 Anthropic prompt caching 的 cache breakpoint，把以下内容放入可缓存前缀：

1. 系统身份与 Novelborne 规则；
2. 作品世界模型和原著索引；
3. 角色四维 transfer；
4. 稳定的试卷契约和输出 schema。

本回合行动、最新状态、动态锚点和临时目标放在缓存后缀。

必须记录：

- cache creation input tokens；
- cache read input tokens；
- 普通 input/output tokens；
- cache hit/miss；
- cache key/hash。

### 11.3 OpenAI

适配 OpenAI 的 prompt caching 规则：

- 保持稳定前缀完全一致；
- 固定字段顺序和固定 schema 文本；
- 动态内容放到消息尾部；
- 不在稳定前缀中混入时间戳、随机 ID 或每回合变化的状态；
- 对 Responses API 和 Chat Completions API 分别封装，不让业务层感知差异。

### 11.4 本地语义缓存

供应商缓存之外增加本地缓存：

- 原著检索结果；
- 世界模型；
- 章节摘要；
- 角色反应候选；
- 机械校验结果；
- 相同 snapshot + action 的短期候选计划。

本地缓存必须绑定：

```text
provider + model + prompt_version + source_hash + state_hash + request_kind
```

任何世界书、角色卡、提示词版本或核心规则变化，都应使旧缓存失效。

### 11.5 缓存安全

- API key 只从环境变量或运行时配置读取；
- 缓存内容不写入公开日志；
- 私有原著和角色内容不能上传到不应接收的缓存服务；
- 存档中只保存缓存引用和版本哈希，不保存供应商密钥；
- 缓存命中不能绕过状态校验。

---

## 十二、响应时间预算

目标不是减少总计算量，而是限制关键路径：

| 阶段 | 目标耗时 | 说明 |
|---|---:|---|
| 快照与本地检索 | 1–5 秒 | 本地索引优先 |
| 候选推演 Wave 1 | 10–40 秒 | 3–6 路并发 |
| 仲裁与正式蓝图 | 5–20 秒 | 短 JSON |
| 段落/选项生成 | 20–90 秒 | 并发 |
| 批改与局部重填 | 10–90 秒 | 只修失败槽位 |
| 提交、摘要、落盘 | 1–5 秒 | 纯本地 |
| 总计 | 30–210 秒 | 复杂回合允许接近当前 5 分钟 |

如果供应商不支持真正并行，应设置最大并发数和超时，并保留确定性兜底；不能无限等待。

---

## 十三、持久化与可恢复性

每回合至少落盘以下信息：

- `turn_id`；
- before/after state hash；
- 选中的计划 ID；
- 本回合事件列表；
- 使用的原著证据引用；
- 目标层推进情况；
- 正文和完整 A-F 选项；
- 状态 patch 摘要；
- 校验报告；
- 模型 usage 和缓存命中信息；
- 重填次数和最终评分。

提交采用两阶段：

```text
生成临时结果 → 全量验证 → 原子提交 durable state
```

失败时：

- 恢复 history、options、round、chapter_round、save_stage；
- 清理半成品事件；
- 保留失败诊断，但不把失败结果冒充成功回合；
- 不触发后续 milestone。

---

## 十四、实施阶段

### 阶段 1：抽离 Agent 门面

新增：

- `core/services/story_agent.py`
- `core/services/story_snapshot.py`
- `core/services/story_result.py`

先把现有 `on_send()` 中的回合流程封装进去，保持 API 和前端不变。

验收：现有测试全通过，旧模式仍可运行。

### 阶段 2：统一目标层与许愿兑现

新增：

- `core/engine/narrative_objective.py`
- `core/engine/story_graph.py`

把许愿、任务、碎锚、宿敌和角色目标统一成目标/事件对象。

验收：每个目标都能在正文、选项、状态 patch 和检查报告中追踪。

### 阶段 3：三层世界模型与长程记忆

扩展：

- `context_retrieval_service.py`
- `context_compressor.py`
- `plot_summary.py`
- `turn_blueprint.py`

增加静态世界层、叙事时间层、当前场景层和结构化事件索引。

验收：压缩 10、20、50、100 回合后，锚点、角色、目标、因果和关键事实可检索且不丢失。

### 阶段 4：并行候选推演与局部重填

扩展：

- `parallel.py`
- `turn_pipeline.py`
- `agent_mode.py`
- `turn_grader.py`

实现 3–6 路短计划并发、裁判选择、段落/选项并发、失败槽位局部重填。

验收：质量提升时，端到端耗时不超过当前上限太多；整章重生成次数显著降低。

### 阶段 5：Anthropic/OpenAI 缓存

扩展：

- `model_gateway.py`
- `distill.py`
- 新增 `core/services/prompt_cache.py`

先实现统一接口，再分别实现供应商适配器。

验收：日志可区分 cache hit/miss；同一作品连续回合的稳定前缀不重复计为普通输入；缓存失效条件可验证。

### 阶段 6：灰度切换

增加配置：

```text
STORY_AGENT_MODE=legacy|agent|shadow
STORY_AGENT_ROLLOUT_PERCENT=0-100
STORY_AGENT_MAX_PARALLELISM=...
STORY_AGENT_CACHE_PROVIDER=auto|anthropic|openai|off
```

- `legacy`：原流程；
- `shadow`：新 Agent 生成但不提交，仅记录差异；
- `agent`：新 Agent 正式提交。

---

## 十五、测试与验收指标

### 15.1 确定性测试

- 事件前置依赖；
- 锚点提前/延迟；
- 状态 patch 冲突；
- 角色知情范围；
- 能力冷却与代价；
- 许愿目标和兑现窗口；
- 选项 effect 合法性；
- 压缩后事实保真；
- save/load round-trip；
- cache key 稳定和失效。

### 15.2 FakeModel 测试

覆盖：

- 正常候选计划；
- 候选计划互相矛盾；
- 正文不含证据；
- 选项缺少目标响应；
- 段落超长/过短；
- 模型返回错误 JSON；
- 部分调用超时；
- 局部重填成功；
- 局部重填失败后的整体回滚。

### 15.3 长剧情实验

至少进行：

- 30 回合；
- 50 回合；
- 100 回合。

指标：

1. 回合提交成功率；
2. 锚点连续性；
3. 未完成目标丢失率；
4. 角色一致性评分；
5. 因果冲突数；
6. 选项有效率；
7. 局部重填占比；
8. 平均和 P95 等待时间；
9. cache hit ratio；
10. 每回合输入/输出/cache token。

---

## 十六、推荐的最终形态

最终系统不是“一个更大的 prompt”，而是一个由代码主持的专用剧情 Agent：

```text
代码维护事实、时间、状态、因果、边界和提交事务；
模型负责理解原著、提出剧情候选、表达角色和生成自然语言；
缓存负责复用稳定世界知识；
并行负责把更多推演 token 压进相近的等待时间；
验证器负责拒绝不连贯、不兑现、不合法的结果。
```

最重要的设计原则是：

> **让模型拥有更多推演空间，但不给模型独自拥有世界事实的权力。**

这样才能同时获得更强的长剧情能力、更好的角色表现、更可靠的许愿兑现，以及不会随回合增长而快速失控的稳定性。

---

## 十七、剧情选项质量的进一步强化

当前 `core/services/options_service.py` 已具备结构化输出、A-F 分配、4+2 因素分布、相似度去重、蓝图节拍注入和 best-of-2，但这些机制主要保证“格式合格”，还不能充分保证选项具有真正的戏剧价值。新 Agent 应把选项视为**可执行的剧情分支契约**，而不是正文之后生成的附属文本。

### 17.1 选项先于正文完成分支设计

正式生成顺序建议调整为：

```text
长期目标/事件图
   ↓
候选分支图（每个分支含行动、代价、收益、风险、后续锚点）
   ↓
导演裁判选择 6 个互补分支
   ↓
正文生成时按分支图埋入可观察线索
   ↓
选项表述生成与验证
```

每项选项应先生成结构化对象：

```json
{
  "key": "A",
  "action": "玩家实际可以做的事",
  "immediate_effect": "本回合立刻变化",
  "cost": "资源/关系/时间/暴露风险",
  "risk": "失败或副作用",
  "information_revealed": "玩家根据正文能知道什么",
  "next_hook": "会推动哪个目标或事件",
  "character_basis": "由哪位角色的性格/利益产生",
  "state_patch_preview": {},
  "required_evidence": []
}
```

### 17.2 选项质量的八个硬维度

每一项至少检查：

1. **可执行性**：玩家当前拥有能力、位置和资源，不能要求不存在的行动条件。
2. **正文可见性**：玩家能从正文推断为何有此选项，不能突然出现“凭空菜单”。
3. **分支差异性**：六项不能只是同义改写，必须改变目标、代价、关系或信息。
4. **代价真实性**：高收益选项必须付出时间、资源、暴露、关系或未来风险中的至少一项。
5. **角色因果**：选项要由当前角色利益、性格或冲突产生，而不是通用模板。
6. **时序合法性**：不能依赖尚未发生的事件、未来知识或当前角色尚未知晓的事实。
7. **状态可落地**：每项预演后的 patch 合法、互相不冲突，且不会绕过能力冷却。
8. **长期牵引**：每项至少连接一个当前目标、锚点、任务、许愿或未结事件；允许一项“自由探索”选项，但不能六项全部游离。

### 17.3 选项反事实模拟

对六项候选分别运行轻量反事实模拟，不生成完整正文，只预测未来 1–3 回合：

- 下回合最可能的事件；
- 角色关系变化；
- 锚点推进/偏离；
- 资源和代价；
- 可能触发的冲突。

代码负责执行状态 patch 试算，模型负责补充叙事后果。最终选项不只展示“后果一句话”，还应内部保留 `counterfactual_trace`，供后续回合使用。

### 17.4 六项不是固定 4+2，而是受约束的多样性集合

保留旧客户端的 6 项 A-F 兼容形状，但内部不应机械地只分成四条金手指、两条性格。建议把分类扩展为：

- 主线推进；
- 角色关系；
- 资源/能力；
- 信息/调查；
- 风险/反转；
- 性格表达。

对外仍可映射旧 `factor` 字段，对内使用多轴标签。通过最小覆盖约束保证至少覆盖：一项低风险、一项高风险、一项关系、一项信息、一项主线推进，以及一项自由或性格行动。

### 17.5 选项选择后的承诺锁

玩家选择 A-F 后，应写入 `choice_commitment`：

- 选择的行动；
- 预期效果；
- 已知风险；
- 本回合必须兑现的最小结果；
- 允许失败的边界。

下一回合正文必须回应该承诺，不能把选项当作无状态按钮。

---

## 十八、导出小说质量与当前导出 Bug

### 18.1 已确认 Bug

当前 `core/server.py:_run_export()` 使用：

```python
segments = exporter.extract_narrative(state)
```

而 `core/engine/novel_exporter.py:extract_narrative()` 只读取当前 state 的 `history`。当前回合如果 `history` 被上下文压缩、截断、替换，或前端只把本回合消息放入 live state，导出就只能看到当前回合或当前保留窗口。

因此出现：

> 正在第 10 回合时，导出只得到第 10 回合；前 1–9 回合无法导出。

这不是小说三遍改写流程本身的问题，而是**导出源选择错误**：把运行时对话 history 当成了完整小说事实库。

### 18.2 正确的数据源

每次回合提交时，必须同时写入不可压缩的 `story_ledger` 或 `chapters`：

```json
{
  "round": 10,
  "chapter": 2,
  "chapter_round": 4,
  "choice": "B",
  "narrative": "本回合完整正文",
  "options": [{"key": "A", "text": "..."}],
  "events": [],
  "state_hash": "..."
}
```

`history` 只用于模型上下文；`story_ledger` 才是导出、回放、审计和长程记忆的事实源。压缩 history 时绝不能删除 story ledger。

### 18.3 导出修复方案

1. 在状态初始化时创建 `story_ledger=[]`，旧存档恢复时从已有 `history` 尽力迁移。
2. 每次成功提交回合后，以 `round/turn_id` 幂等追加完整正文到 ledger。
3. `_run_export()` 优先读取 `story_ledger`，按 `round`、`chapter`、`chapter_round` 排序；只有旧存档没有 ledger 时才回退到 history。
4. 导出源必须包含所有已提交回合，而不是当前 history 尾部。
5. 导出前检查：
   - 回合编号是否连续或明确记录缺口；
   - 正文是否为空；
   - 是否存在重复 round；
   - 是否包含当前回合之前的历史正文；
   - `full_text` 是否覆盖全部已提交回合。
6. 导出结果 manifest 增加：
   - `source_round_min`；
   - `source_round_max`；
   - `source_turn_count`；
   - `source_gaps`；
   - `source_kind=story_ledger|history_fallback`。
7. 发生缺口时不能静默导出“看起来完整”的小说，应明确返回缺失回合和可导出的范围。

### 18.4 小说导出不应简单地逐章三遍重写

现有 `novel_exporter.py` 的 plot → style → final_polish 结构适合作为基础，但长篇导出还需要：

- **全书结构编辑**：先生成卷/章级目录、人物弧线和伏笔回收表；
- **章节级改写**：每章只处理本章正文和相邻章节 handoff；
- **跨章一致性审查**：检查称谓、时间、伤势、物品、关系、能力和视角；
- **伏笔回收审查**：确认已埋线索在后文是否回收，未回收线索不能被 final polish 擅自删除；
- **最终连续性修订**：只修订冲突章节，不整本重写。

建议导出流水线：

```text
story_ledger
  ↓
全书索引与时间线
  ↓
卷/章结构规划
  ↓
章节并行改写
  ↓
跨章一致性审查
  ↓
问题章节局部修订
  ↓
全书终稿组装
```

### 18.5 导出质量门

导出后必须运行独立 `NovelContinuityReport`：

- 章节顺序；
- round 覆盖率；
- 人物首次/最后出现；
- 关键关系变化；
- 物品和能力获得/消耗；
- 时间和地点跳变；
- 视角一致性；
- 重复段落；
- 残留“选项/回合/系统”痕迹；
- 伏笔状态；
- 全文字符数和非空章节数。

只有报告通过，才允许标记 `export_ok=true`。

### 18.6 导出与运行时事实分离

小说导出可以文学化改写，但不能改变事实。应保留两个版本：

- `canonical_story`：来自 story ledger 的不可变事实层；
- `literary_export`：经过 plot/style/final polish 的阅读版本。

任何文学稿都应能追溯到 ledger 的回合和事件。导出失败或改写失败时，至少可以输出忠实整理版，而不是丢失历史。

---

## 十九、需要追加的测试

### 19.1 选项测试

- 六项语义差异足够；
- 每项能通过状态预演；
- 选项不使用未来知识；
- 正文线索覆盖选项理由；
- 至少一项推进当前目标；
- 至少一项包含真实代价；
- 选择后承诺写入并在下一回合兑现；
- 六项候选互相不产生非法 patch。

### 19.2 导出回归测试

构造 10 回合 state：

- history 只保留压缩后的摘要和最近 4 条；
- story ledger 保留完整 10 回合；
- 调用 `/api/sessions/{session_id}/export-novel`；
- 断言导出 manifest 为 `source_kind=story_ledger`；
- 断言 `source_round_min=1`、`source_round_max=10`、`source_turn_count=10`；
- 断言 full_text 包含第 1 至第 10 回合正文；
- 断言不会只导出第 10 回合。

同时覆盖：

- 旧存档无 ledger 的迁移；
- ledger 有重复 round；
- ledger 有缺口；
- 当前回合尚未 committed；
- 导出模型中途失败；
- 单章失败后的局部重试；
- 导出全文与 manifest 数量不一致。

---

## 二十、补充结论

当前架构距离专用剧情 Agent 已经不远，但还需要补上两个关键缺口：

1. **选项必须从“生成文本”升级为“可执行反事实分支”**；
2. **小说导出必须从 history 迁移到不可压缩的 story ledger**。

前者决定玩家是否感到每个选择真的有意义，后者决定长剧情是否能够完整保存和出版式导出。两者都不应靠更长 prompt 解决，而应由结构化状态、事件图、反事实模拟、提交事务和独立质量报告共同保证。

---

## 二十一、上下文窗口预算：200k 黄金区间

### 21.1 基本原则

总预算不设固定上限，但**单次模型请求必须有上下文预算管理**。目标不是把每次请求都塞满 200k，而是让完整剧情 Agent 在需要全局视野的请求中稳定工作在约 200k 的黄金区间，同时为输出和重试保留安全余量。

建议默认预算：

```text
模型上下文上限：由 provider/model discovery 得到
目标输入上下文：160k–185k
输出预留：12k–24k
安全余量：8k–16k
软目标总窗口：约 180k–200k
```

如果供应商模型上限低于 200k，自动降级到该模型可承受范围，不允许硬塞导致截断。若模型上限远高于 200k，也不默认把全部历史送入模型；超出 200k 后收益通常下降，噪声和注意力稀释会上升。

### 21.2 四级上下文装配器

新增 `ContextBudgetManager`，由代码按优先级装配：

1. **P0 不可裁剪事实**：当前 state hard facts、已提交事件、当前目标、角色边界、能力冷却、已选承诺、当前锚点。
2. **P1 高价值连续性**：最近 8–12 回合正文摘要、最近 2–4 回合原文、未结因果、近期关系变化、当前章原文窗口。
3. **P2 可检索世界知识**：与当前任务相关的原著片段、角色证据、地点/组织资料、未来锚点索引。
4. **P3 低优先级背景**：远期原文、已闭合支线、重复描述、旧选项和已兑现目标。

预算不足时从 P3 向 P2、P1 逐层压缩，P0 不能被静默删除。每次装配都生成：

```json
{
  "target_total": 200000,
  "estimated_input": 173420,
  "reserved_output": 18000,
  "safety_margin": 8560,
  "dropped_blocks": [],
  "truncated_blocks": [],
  "context_hash": "..."
}
```

### 21.3 上下文不是一个大字符串

使用带标签的 `PromptLayers`：

```python
@dataclass
class PromptLayers:
    system_rules: str
    world_facts: str
    timeline: str
    character_models: str
    active_objectives: str
    retrieved_evidence: str
    recent_narrative: str
    current_action: str
    output_contract: str
```

每层拥有：

- token 估算；
- 来源 hash；
- 新鲜度；
- 是否可裁剪；
- 是否可缓存；
- 所属优先级。

这样可以保证静态部分始终保持完全一致，利于 Anthropic/OpenAI 缓存命中，同时允许动态层增量替换。

### 21.4 上下文压缩必须保留“事实索引”

`context_compressor.py` 的摘要不能只生成散文。每次压缩应同时产出：

- `fact_index`：已确认事实；
- `event_index`：事件及其状态；
- `causal_open_loops`：未完成因果环；
- `character_delta_index`：角色状态差分；
- `objective_index`：许愿/任务/碎锚兑现进度；
- `timeline_index`：时间与地点序列；
- `lexical_evidence`：关键逐字证据。

散文摘要服务于模型可读性，结构化索引服务于代码校验和检索。两者缺一不可。

### 21.5 上下文预算与并发

不同子 Agent 不应全部携带完整 200k：

- 战略规划 Agent：100k–180k，侧重全局时间线和候选路径；
- 正文段落 Agent：30k–80k，侧重当前场景和局部证据；
- 选项 Agent：40k–100k，侧重状态、当前正文线索和反事实分支；
- 质量裁判 Agent：20k–60k，侧重结构化候选和错误证据；
- 全局导出编辑 Agent：160k–200k，按卷或章节窗口处理。

这比让每个调用都传 200k 更快，也更省输入 token，同时保留一个真正拥有全局视野的战略层。

### 21.6 超时策略

总 token 无上限不等于等待无上限。为每回合设置 deadline：

```text
soft deadline：当前回合通常目标时间
hard deadline：用户可接受的最长等待时间
cutoff：停止新增候选，提交当前最佳合法结果
```

到达 soft deadline 后：

- 不再增加候选数；
- 停止低价值反思；
- 已有候选进入并行验证；
- 只允许针对硬错误的重填。

到达 hard deadline 前：

- 取消未完成的低优先级请求；
- 保留已通过硬门禁的最佳候选；
- 必要时使用确定性组装或低成本修复；
- 绝不因为等待更多 token 而阻塞提交。

---

## 二十二、专门的“类 Agent 选项”通路

前台仍保持现有接口：继续返回 A-F 选项、`text`、`preview`、`factor` 等字段。`类 Agent` 只是后端内部代号，不能出现在用户 UI 或公共 API 契约中。

### 22.1 选项通路与正文通路分离

新增内部服务，例如：

```text
core/services/choice_agent.py
```

它不是简单调用 `options_service.generate_options()`，而是一个短周期规划器：

```text
读取 snapshot
  ↓
目标与事件筛选
  ↓
角色动机/可用资源分析
  ↓
生成 8–16 个候选行动
  ↓
代码模拟状态与时序
  ↓
模型反事实预测 1–3 回合
  ↓
风险/代价/多样性裁判
  ↓
选择 6 个最终分支
  ↓
生成前台兼容 A-F 文案
  ↓
正文线索覆盖检查
```

### 22.2 候选行动的内部结构

```json
{
  "candidate_id": "c-17",
  "action": "行动本身",
  "actor": "玩家",
  "motivation": "为什么此刻会产生这个行动",
  "preconditions": [],
  "state_patch": {},
  "cost": {},
  "risk": {},
  "timeline_effect": {},
  "objective_links": [],
  "character_reactions": [],
  "counterfactual": [],
  "evidence_terms": [],
  "novelty_signature": "..."
}
```

### 22.3 多模型角色分工

为了提高质量而不拉长关键路径，选项专用通路可并行使用不同职责：

- **探索器**：提出大量可能行动；
- **规则器**：代码检查前置条件、状态 patch、时序和能力边界；
- **反事实器**：预测未来 1–3 回合的后果；
- **戏剧裁判**：评估张力、差异、代价和玩家 agency；
- **文案器**：将内部结构压缩成前台 A-F 文本。

其中探索器和反事实器可以多路并发，规则器尽量本地确定性执行，戏剧裁判只读取短结构化候选。

### 22.4 选项与正文双向约束

当前流程是“正文和选项并发生成”，容易产生选项没有正文依据的问题。新通路应改成：

1. 先确定候选分支的 `required_evidence`；
2. 正文生成时埋入必要线索，但不能显露菜单格式；
3. 选项文案根据实际正文生成；
4. 代码检查每个选项的行动理由在正文中可追溯；
5. 若某个分支缺少正文证据，只重写该分支或补充对应段落，不重写全部正文。

### 22.5 选项集合级评分

单项合格不等于六项整体优秀。集合级评分至少包括：

- 风险梯度；
- 目标覆盖；
- 角色覆盖；
- 信息覆盖；
- 低/中/高代价分布；
- 短期与长期后果分布；
- 同义行动相似度；
- 是否存在明显“正确答案”；
- 是否保留玩家自由度；
- 是否与当前锚点和世界逻辑一致。

最终只把通过集合级评分的六项映射为 A-F。

### 22.6 低延迟提交策略

选项通路可以在正文初稿完成前先生成内部候选，但最终文案必须等待正文证据检查。为控制延迟：

- 探索与正文段落并发；
- 反事实只对前 8 个候选运行；
- 裁判只处理经过代码筛选的 4–8 个候选；
- 文案生成可以使用短上下文；
- 失败只重填单个选项；
- 已有 6 个合法且集合评分达标时立即停止继续探索。

---

## 二十二点七、全文式先验驱动的剧情规划

由于作品全文在开局时已经给定，系统面对的主要不确定性不是“世界将发生什么”，而是“玩家输入如何改变已知剧情轨道”。因此应建立 **Full-Text Prior Planner**，把原著未来走向转化为带置信度的可偏移先验，而不是每回合从零猜测。

### 22.7.1 先验分层

全文蒸馏后将未来内容分为：

- `hard_anchor`：关键节点、不可随意删除的世界事实；
- `soft_trajectory`：大概率发生但允许玩家改变的主线走向；
- `branch_opportunity`：玩家输入可以改变的分叉点；
- `foreshadowing`：应提前铺垫的伏笔；
- `spoiler_guard`：未来结果的隐藏信息，不直接暴露给玩家。

每个事件记录：

```json
{
  "event_id": "evt-102",
  "source_chapter": 18,
  "expected_window": [16, 20],
  "prior_probability": 0.82,
  "anchor_strength": 0.74,
  "branchability": 0.55,
  "spoiler_level": 0.8,
  "required_setup": [],
  "payoff_events": [],
  "evidence_refs": []
}
```

### 22.7.2 玩家输入作为扰动量

每次用户输入先经过 `InputInterpretation`：

- 意图分类；
- 作用对象；
- 改变的目标；
- 是否引入新行动；
- 是否与原著先验冲突；
- 是否触发分叉点；
- 对时间线的影响范围。

然后计算：

```text
new_event_score = prior_score × continuity_score × player_intent_score × feasibility_score
```

玩家输入不能直接覆盖全文先验，而是通过“扰动预算”改变局部事件概率和可达路径。

### 22.7.3 未来窗口规划

每回合维护：

- 当前回合窗口：本回合必须处理的事件；
- 近程窗口：未来 1–3 回合的高概率事件；
- 中程窗口：未来 4–12 回合的走向；
- 远程窗口：当前章/卷的主线终点和伏笔回收。

正文只使用当前和近程窗口的可见证据；中程与远程只约束规划器，不直接泄漏结果。

### 22.7.4 先验与玩家自由的平衡

事件先验不是强制剧情脚本。代码根据：

- `anchor_strength`；
- `branchability`；
- 玩家输入影响强度；
- 当前状态可行性；
- 已发生偏离程度；

动态决定：

1. 回归原著轨道；
2. 延迟事件；
3. 改写事件；
4. 创建平行支线；
5. 允许彻底偏离并生成新的局部先验。

只有 `hard_anchor` 且未被合法机制打破时，才允许提高回归权重；不能因“原著如此”直接否定玩家行动。

---

## 二十二点八、历史事件动态赋权机制

### 22.8.1 事件权重的组成

每个已发生事件在当前回合拥有动态权重：

```text
W_event =
  W_recency
× W_causality
× W_unresolved
× W_character_impact
× W_objective_link
× W_anchor_relevance
× W_evidence_strength
× W_player_salience
```

建议每项归一化到 0–1，再通过对数或 softmax 防止单项无限放大。

### 22.8.2 各项权重定义

- `recency`：距离当前回合越近越高，但重要事件有衰减下限；
- `causality`：当前事件是否直接依赖它；
- `unresolved`：未收束事件、未兑现目标和悬念权重更高；
- `character_impact`：改变核心角色状态的事件更高；
- `objective_link`：与许愿、任务、碎锚和承诺锁相关时更高；
- `anchor_relevance`：与当前/下一个锚点的图距离越短越高；
- `evidence_strength`：有多条原文和状态证据时更高；
- `player_salience`：玩家明确提及、选择或反复关注时提高。

### 22.8.3 动态更新

每次回合提交后更新：

```python
weight_t1 = clamp(
    weight_t * decay
    + event_delta
    + objective_bonus
    + player_attention_bonus
    - resolution_penalty,
    0.0, 1.0,
)
```

事件完成后不删除，而是进入 `resolved_memory`，保留较低但非零的回溯权重，防止人物突然忘记关键历史。

### 22.8.4 历史压缩中的保权

压缩 history 时不能按字符截断。必须按 `W_event` 选择：

- 高权重事件：保留完整事实和证据；
- 中权重事件：保留摘要和因果关系；
- 低权重事件：只保留索引和可检索引用。

这保证长剧情不会被最近几回合噪声完全淹没。

---

## 二十二点九、生成因素的动态赋权

### 22.9.1 因素统一表示

将当前生成因素统一为 `GenerationFactor`：

```json
{
  "factor_id": "anchor.current",
  "kind": "anchor",
  "value": "当前锚点事件",
  "weight": 0.91,
  "hardness": "hard",
  "source": "story_graph",
  "evidence": [],
  "conflicts": [],
  "decay": 0.02,
  "boost": 0.15
}
```

因素来源包括：

- 原著先验；
- 当前锚点；
- 历史事件；
- 玩家输入；
- 角色目标；
- 许愿；
- 任务；
- 金手指；
- 宿敌；
- 世界书；
- 场景证据；
- 选项承诺。

### 22.9.2 权重计算

建议采用“基础权重 × 动态修正 × 冲突抑制”：

```text
W_factor = base
× recency_boost
× causal_boost
× objective_boost
× evidence_boost
× player_boost
× branch_fit
× conflict_penalty
```

其中：

- `hardness` 决定是否允许被压低；
- `evidence` 决定模型是否能把因素写成正文；
- `branch_fit` 衡量与玩家当前行动的匹配度；
- `conflict_penalty` 防止互相矛盾的因素同时占据高权重。

### 22.9.3 不同输出阶段使用不同权重

同一因素不能在所有阶段用同一权重：

- 战略规划：重视远期先验、事件图和未结因果；
- 正文生成：重视当前场景、角色动机和证据；
- 选项生成：重视玩家可执行性、风险和分支覆盖；
- 状态 patch：重视硬规则、前置条件和状态合法性；
- 文学润色：降低机制字段权重，只保留事实约束。

每个阶段都生成 `effective_weight(stage, factor)`，并记录在审计中。

### 22.9.4 权重不是模型自由决定的

模型可以提出权重理由，但最终权重由代码计算。模型不得通过把某项描述得更激烈而自动提升其优先级。代码必须能够回答：

- 为什么本回合突出这个事件；
- 为什么另一个因素被压低；
- 哪些证据支持该权重；
- 哪些冲突导致降权；
- 下一回合权重预计如何变化。

---

## 二十二点十、连接不畅与 Agent 鲁棒性

### 22.10.1 模型调用分级

所有调用分为：

- `critical`：状态裁判、最终 patch、提交前验证；
- `important`：战略计划、正文主段、选项集合；
- `optional`：额外反思、风格候选、低价值扩展。

连接不畅时按 `optional → important → critical` 的顺序释放资源，critical 不得静默跳过。

### 22.10.2 断线处理

`model_gateway` 增加：

- 指数退避；
- 抖动；
- 每请求连接超时、首字节超时、整体超时；
- provider 错误分类；
- 幂等 request id；
- 熔断器；
- 半开探测；
- 取消未完成并行任务。

### 22.10.3 并行调用的部分成功

每个子任务必须可独立完成和审计：

- 成功结果进入候选池；
- 超时结果标记 `timeout`；
- 连接错误标记 `transport_error`；
- 解析错误标记 `schema_error`；
- 不因一个非关键任务失败而重跑全部任务。

### 22.10.4 多供应商与缓存降级

优先使用当前 provider；连接异常时：

1. 重试同 provider；
2. 使用同 provider 缓存结果；
3. 使用已完成的并行候选；
4. 若用户配置了备用 provider，切换备用 provider；
5. 使用确定性校验和局部修复；
6. 没有安全候选则回滚。

备用 provider 不能自动拿到未经允许的私有原著或密钥配置。

### 22.10.5 断点续跑

复杂回合按 stage 保存内部 checkpoint：

```text
retrieval_done
plan_candidates_done
plan_selected
segments_done
options_done
validation_done
```

连接恢复后只从最近 checkpoint 继续，不重复已成功的高 token 阶段。checkpoint 只保存脱敏结构化结果和哈希，不保存密钥。

### 22.10.6 鲁棒性验收

注入以下故障：

- 单请求超时；
- 连接断开；
- 返回截断 JSON；
- 429/5xx；
- 并发中一半失败；
- 缓存服务不可用；
- 备用 provider 不可用。

必须证明：

- 不提交半成品；
- 不重复推进 round；
- 不丢失 ledger；
- 不重复扣除资源；
- 不无限重试；
- 在 hard deadline 内完成回滚或降级响应。

---

## 二十二点十一、先验与动态权重的综合验收

### 22.11.1 全文先验测试

- 给定完整原著，检查未来锚点顺序；
- 玩家不干预时，主线轨迹接近原著先验；
- 玩家在分叉点干预后，局部事件改变但无因果断裂；
- 强锚点不会被普通输入轻易抹除；
- 可分支事件能够响应玩家行为；
- 未来 spoiler 不出现在前台文本。

### 22.11.2 动态权重测试

- 新事件初始权重合理；
- 玩家连续关注某目标时权重上升；
- 事件解决后权重下降但可回溯；
- 未完成目标不会因压缩消失；
- 冲突因素不会同时获得最高权重；
- 不同生成阶段的 effective weight 不相同。

### 22.11.3 长剧情测试

运行 30/50/100 回合并检查：

- 先验轨道偏离是否可解释；
- 历史高权重事件是否持续影响后续剧情；
- 低权重事件是否不占用过多上下文；
- 玩家输入是否真正改变局部权重；
- 断线后是否能从 checkpoint 继续；
- 100 回合后导出仍覆盖全部 ledger。

---

## 二十二点十二、增加后的实施顺序

在原施工方案基础上，增加以下顺序：

1. 先完成全文切章、事件抽取、证据引用和先验索引；
2. 再完成事件图和目标层；
3. 再实现历史事件权重与生成因素权重；
4. 再接入 200k 上下文预算装配；
5. 再实现断线重试、熔断、checkpoint 和部分成功汇合；
6. 再实现 choice agent 的候选、反事实和集合评分；
7. 最后接入正式正文生成、导出和 Agent 主流程。

原因是：没有全文先验和动态权重时，后续 Agent 只能增加调用次数，无法真正提高长剧情方向判断；没有 checkpoint 和故障分级时，增加并行调用反而会放大连接故障。


### 23.1 叙事不变量

为长剧情维护不可随意违反的不变量：

- 已确认死亡/离场角色不能无解释出现；
- 已消耗物品不能重复使用；
- 能力冷却未结束不能再次使用；
- 未知信息不能被不知情角色引用；
- 已承诺的玩家行动必须得到回应；
- 已完成任务不能退回 active；
- 当前时间不能倒退，除非明确进入回溯机制；
- 未来锚点只能按事件图推进。

### 23.2 叙事静态分析

每回合提交前，把正文、选项和 patch 转为中间表示，运行类似编译器的检查：

```text
解析实体 → 解析事件 → 建立引用 → 检查前置依赖 → 模拟状态 → 生成诊断
```

诊断分为：

- error：不得提交；
- warning：可提交但记入质量报告；
- info：用于后续检索和摘要。

### 23.3 记忆回写不是摘要，而是事件溯源

每回合提交后同时写：

- immutable turn record；
- event append log；
- state snapshot/hash；
- narrative ledger；
- retrieval indexes；
- cache references。

任何摘要都可以重建，不能反过来成为唯一事实来源。

---

## 二十四、更新后的实施优先级

1. 先修复 `story_ledger` 和导出全量回合 bug；
2. 抽离 `StoryAgent` 回合事务门面；
3. 实现 `ContextBudgetManager` 和分层 PromptLayers；
4. 建立事件图、目标层和叙事不变量；
5. 实现 `choice_agent.py` 专门选项通路，保持前台契约不变；
6. 加入供应商缓存适配与命中审计；
7. 增加 30/50/100 回合长程实验；
8. 通过 shadow 模式比较旧流程与新流程；
9. 最后再正式切换默认 Agent 路径。

最终要求：

> **单次调用控制在约 200k 的有效上下文区间，整体 token 可以继续增加；用并行和缓存换质量，用 deadline 换响应稳定，用代码事件图和不变量守住剧情事实；前台仍只看到原来的 A-F 选项和小说导出接口。**

---

## 二十五、可直接实施的模块拆分

本方案应按“新增中间层、逐步接管旧入口”的方式实施，不直接重写 `core/app.py` 和 `core/server.py` 的全部逻辑。

### 25.1 推荐新增模块

```text
core/services/story_agent.py              # 专用剧情 Agent 总编排器
core/services/story_snapshot.py           # 回合快照与 hash
core/services/story_context.py            # PromptLayers 与 200k 预算装配
core/services/choice_agent.py             # 专门的内部选项通路
core/services/continuity_agent.py         # 长程连续性审查
core/services/cache_service.py            # 本地语义缓存
core/services/provider_cache.py           # Anthropic/OpenAI 适配
core/engine/story_graph.py                # 事件图与时间约束
core/engine/narrative_objective.py        # 统一目标层
core/engine/story_invariants.py           # 叙事不变量
core/engine/counterfactual.py             # 选项反事实模拟
core/engine/story_ledger.py               # 不可压缩故事事实账本
core/engine/turn_transaction.py           # 回合事务与原子提交
core/engine/export_continuity.py         # 导出连续性报告
```

### 25.2 旧模块的职责调整

| 当前模块 | 调整后的职责 |
|---|---|
| `core/app.py` | 保留兼容入口，只负责组装请求、流式输出和调用 Agent |
| `core/server.py` | 保留 API 契约，调用 Session/StoryAgent，不承载剧情决策 |
| `turn_pipeline.py` | 变成“正式正文生成器”，不再负责全局策略 |
| `options_service.py` | 保留前台 A-F 序列化和清洗，候选分支改由 `choice_agent.py` 提供 |
| `context_compressor.py` | 负责生成摘要和结构化索引，不删除 ledger |
| `novel_exporter.py` | 读取 ledger，负责文学化流水线，不再直接依赖 history |
| `model_gateway.py` | 统一 provider、超时、usage、缓存和取消 |
| `distill.py` | 继续作为模型调用 choke point，不被业务模块绕过 |

---

## 二十六、核心数据契约

### 26.1 StorySnapshot

```python
@dataclass(frozen=True)
class StorySnapshot:
    session_id: str
    round: int
    chapter: int
    chapter_round: int
    state_hash: str
    state_facts: dict[str, Any]
    active_members: tuple[dict[str, Any], ...]
    current_anchor: dict[str, Any] | None
    upcoming_anchors: tuple[dict[str, Any], ...]
    active_objectives: tuple[dict[str, Any], ...]
    open_events: tuple[dict[str, Any], ...]
    recent_turns: tuple[dict[str, Any], ...]
    source_hash: str
    prompt_version: str
```

快照只允许从 state 构造，不能让模型直接修改。`state_hash` 使用规范化 JSON 计算；同一状态必须得到同一 hash，供缓存和幂等提交使用。

### 26.2 TurnPlan

```python
@dataclass
class TurnPlan:
    plan_id: str
    snapshot_hash: str
    beat: str
    goal: str
    conflict: str
    events: list[dict[str, Any]]
    segment_plans: list[dict[str, Any]]
    option_candidates: list[dict[str, Any]]
    required_evidence: list[str]
    forbidden_events: list[str]
    objective_updates: list[dict[str, Any]]
```

`TurnPlan` 是本回合唯一正式施工图。正文、选项、状态 patch 和质量检查都必须引用同一个 `plan_id`。

### 26.3 TurnCandidate

```python
@dataclass
class TurnCandidate:
    narrative: str
    options_internal: list[dict[str, Any]]
    public_options: list[dict[str, Any]]
    events: list[dict[str, Any]]
    state_patch: dict[str, Any]
    evidence: list[dict[str, Any]]
    usage: dict[str, Any]
    provenance: dict[str, Any]
```

`public_options` 仅用于兼容前台；`options_internal`、反事实轨迹和规则诊断可以保存在内部审计中，不直接暴露给 UI。

### 26.4 ValidationReport

```python
@dataclass
class ValidationReport:
    ok: bool
    errors: list[dict[str, Any]]
    warnings: list[dict[str, Any]]
    scores: dict[str, float]
    repaired_slots: list[str]
    invariant_results: list[dict[str, Any]]
    objective_results: list[dict[str, Any]]
    continuity_results: dict[str, Any]
```

提交只看 `ok` 和错误级别；warning 不得被模型自行改写成成功。

---

## 二十七、精确调用时序

### 27.1 回合开始

1. Session 锁定，读取 durable state。
2. 执行旧存档迁移和 ledger 补齐检查。
3. 构造 `StorySnapshot`，计算 `state_hash`。
4. 创建 `TurnTransaction`，保存 before 状态摘要。
5. 启动 deadline 和取消令牌。

### 27.2 Wave 0：本地准备

并行执行：

- 原著检索；
- 世界书命中；
- 当前/未来锚点读取；
- 角色四维摘要读取；
- 未完成目标读取；
- 事件图可推进事件筛选；
- 稳定 PromptLayers 缓存查询。

这一波尽量不调用远端模型。

### 27.3 Wave 1：战略推演

并发生成 3–6 个 `StrategicCandidate`，每个只允许返回结构化 JSON：

```json
{
  "goal": "本回合目标",
  "events": [],
  "causal_chain": [],
  "character_moves": [],
  "risks": [],
  "required_evidence": [],
  "forbidden_events": []
}
```

代码立即过滤：

- 事件前置不满足；
- 使用未知信息；
- 修改不可变事实；
- 超出当前章节允许范围；
- 与已锁定玩家承诺冲突。

### 27.4 Wave 2：计划仲裁

仲裁器只读取候选 JSON 和诊断，不携带完整原著。输出：

- 选中的 `plan_id`；
- 需要合并的事件；
- 被拒绝候选及原因；
- 本回合必须出现的证据；
- 本回合严禁发生的事件。

### 27.5 Wave 3：正文与选项并行

正文段落生成、角色反应生成、选项候选生成并发，但共享 `TurnPlan`。

注意：选项候选可以先于正文完成内部结构，但公共选项文字必须在正文初稿可用后生成，确保选项都有正文依据。

### 27.6 Wave 4：验证与局部修复

验证顺序固定：

```text
JSON/schema → 状态 patch → 时间线 → 事件因果 → 角色边界
→ 正文证据 → 选项集合 → 质量评分 → 导出账本写入
```

每项错误附带 slot 定位。优先修复最小范围：

1. 单个选项；
2. 单个正文段；
3. 单个状态 patch；
4. 必要时才重跑整回合。

### 27.7 Wave 5：提交

1. 在临时 state 上应用 patch。
2. 运行全量不变量检查。
3. 追加 `story_ledger`、event log 和 turn record。
4. 原子替换 durable state。
5. 写入 `after_state_hash`。
6. 释放 Session 锁。

---

## 二十八、模型调用与上下文预算的精确规则

### 28.1 预算计算

每次调用前使用：

```text
available_input = provider_limit - max_output - safety_margin
```

建议：

- `provider_limit`：从模型配置或能力发现读取；
- `max_output`：按任务类型固定上限；
- `safety_margin`：至少 8k，长输出至少 16k；
- 目标输入不超过 185k，除非模型本身上限不足。

### 28.2 预算装配伪代码

```python
def build_context(layers, provider_limit, output_budget):
    budget = min(185_000, provider_limit - output_budget - 8_000)
    selected = []
    used = 0
    for layer in sorted(layers, key=lambda x: x.priority):
        if layer.required:
            selected.append(layer)
            used += layer.estimated_tokens
    for layer in optional_layers_by_value(layers):
        if used + layer.estimated_tokens <= budget:
            selected.append(layer)
        else:
            selected.append(compress_or_trim(layer, budget - used))
    return PromptBundle(selected=selected, budget=budget)
```

实现时必须检测 `used > budget`，不得依赖供应商自动截断。

### 28.3 200k 不是每个子任务的默认值

只有战略规划和全局连续性审查可以接近 200k。段落、选项和裁判使用更小上下文，减少并行请求的总输入成本。

### 28.4 截止策略

每一回合建立：

```python
DeadlineBudget(
    soft_deadline=90,
    hard_deadline=240,
    max_parallel_jobs=12,
    max_repairs=2,
)
```

复杂模型可由配置调整，但硬截止后必须：

- 取消尚未开始的低优先级任务；
- 保留已通过硬门禁的候选；
- 不等待质量分数继续提升；
- 若没有可提交候选，则按现有回滚规则失败，而不是提交半成品。

---

## 二十九、选项专用通路的实施细节

### 29.1 内部状态机

```text
DISCOVER
→ PROPOSE
→ RULE_FILTER
→ COUNTERFACTUAL
→ DIVERSITY_SELECT
→ EVIDENCE_ALIGN
→ COPYWRITE
→ PUBLIC_GRADE
→ READY
```

任一阶段失败只回到最近可修复阶段，不从头重跑。

### 29.2 候选数量和淘汰

- 初始候选：8–16；
- 规则过滤后：至少 6，最好保留 8；
- 反事实预测：前 8 个；
- 集合选择：6 个；
- 文案重填：只对不合格项重填，最多 2 次。

### 29.3 分支覆盖矩阵

选择六项时，建立覆盖矩阵：

```text
行：候选选项
列：主线 / 关系 / 信息 / 资源 / 风险 / 性格 / 低代价 / 高代价
```

使用加权最大覆盖，而不是只按单项分数排序。必须避免六项集中在同一列。

### 29.4 选项状态预演

每个选项先在临时状态执行：

- patch schema；
- 资源变化；
- 时间推进；
- 任务和目标变化；
- 关系变化；
- 事件图变化。

预演失败的选项不能通过只改文案绕过，必须修改内部行动或删除该候选。

### 29.5 前台兼容

对外保持现有：

```json
{"key":"A", "text":"...", "preview":"...", "factor":"..."}
```

内部字段通过 state/agent_meta 私有记录，前端 serializer 默认丢弃；不能改变现有 A-F、自由输入和旧客户端行为。

---

## 三十、导出 Bug 的落地修复顺序

### 30.1 第一优先级：保存完整回合

在回合提交成功处增加唯一函数：

```python
def append_story_turn(state, turn_result, *, turn_id):
    ...
```

要求：

- 按 `turn_id` 幂等；
- round 重复时拒绝覆盖；
- 保存完整正文，不保存 preview；
- 保存完整 A-F 选项和结构化事件；
- 只有 committed 回合才能追加。

### 30.2 第二优先级：旧存档迁移

迁移顺序：

1. 读取已有 `story_ledger`；
2. 若为空，读取 history 中可识别的 assistant 回合；
3. 以回合标记、提交记录或消息顺序推断 round；
4. 无法可靠识别的部分标记 `migration_uncertain=true`；
5. 不把不确定历史伪装为完整成功记录。

### 30.3 第三优先级：改造 `_run_export`

导出源选择：

```text
story_ledger 非空且存在 committed 回合 → ledger
否则 → history fallback
```

manifest 增加：

```json
{
  "source_kind": "story_ledger",
  "source_round_min": 1,
  "source_round_max": 10,
  "source_turn_count": 10,
  "source_gaps": [],
  "committed_only": true
}
```

### 30.4 第四优先级：导出阶段并行化

- 先生成全书目录和连续性索引；
- 章节 plot/style/final polish 可按章并行；
- 跨章审查集中处理；
- 只重写发生冲突的章节；
- 使用缓存复用稳定作品背景和角色卡。

---

## 三十一、缓存实现细节

### 31.1 缓存键

缓存键必须由以下字段规范化后计算：

```text
provider
model
prompt_version
system_rules_hash
work_source_hash
character_schema_version
chapter_index
state_facts_hash
request_kind
```

动态玩家行动不应进入静态前缀缓存键；它应进入请求后缀或本地短期候选缓存。

### 31.2 缓存层级

- `L0 provider_prefix`：系统规则、输出契约；
- `L1 world_model`：作品设定、原著索引；
- `L2 character_context`：角色四维 transfer；
- `L3 chapter_context`：章节窗口、锚点证据；
- `L4 local_candidate`：同状态同动作的短期候选。

### 31.3 失效规则

以下任一变化必须失效对应缓存：

- 原著 source hash；
- 世界书版本；
- 角色卡版本；
- prompt version；
- 规则版本；
- 模型名称；
- provider；
- 章节索引；
- 关键状态事实。

### 31.4 缓存可观测性

每次调用记录安全元数据：

- `cache_layer`；
- `hit/miss`；
- `cache_key_hash`；
- `input_tokens`；
- `cached_input_tokens`；
- `output_tokens`；
- `latency_ms`。

不记录 API key、完整 prompt、私有原著全文或未经脱敏的角色隐私内容。

---

## 三十二、测试矩阵与验收门

### 32.1 单元测试

- StorySnapshot hash 稳定；
- context budget 不超限；
- P0 不被裁剪；
- event graph 前置依赖；
- objective 状态迁移；
- choice patch 预演；
- 选项集合覆盖；
- ledger 幂等追加；
- export source 选择；
- cache key 和失效。

### 32.2 集成测试

- 旧 `on_send` 调 Agent 后前台输出形状不变；
- A-F 选择仍能正常提交；
- 自由输入仍可用；
- 10 回合压缩不损失 ledger；
- save/load 后 Agent 可继续运行；
- OpenAI/Anthropic adapter 均能记录缓存元数据；
- 供应商超时只触发局部降级。

### 32.3 长程验收

每组实验至少记录：

- 30、50、100 回合；
- 每回合输入上下文 token；
- P95 延迟；
- cache hit ratio；
- 锚点冲突数；
- 因果错误数；
- 选项集合评分；
- ledger 覆盖率；
- 导出全量回合覆盖率。

### 32.4 必须满足的硬门

1. 不改变前台 API 和 A-F 交互；
2. 单次请求不得超过 provider 实际上限；
3. 任何 committed 回合都必须进入 ledger；
4. 导出第 N 回合必须覆盖此前所有 committed 回合；
5. 不通过模型自由文本直接修改事实状态；
6. hard deadline 后不得无限等待；
7. 缓存不能跨作品、跨版本污染；
8. 所有失败都能回滚或明确报告。

---

## 三十三、建议的首个可交付切片

不要一次实现全部 Agent。第一个切片应只做以下四件事：

1. `story_ledger`：修复第 10 回合无法导出前 9 回合的问题；
2. `StorySnapshot`：为当前回合建立统一事实快照；
3. `ContextBudgetManager`：只做预算测量、分层和审计，不改变生成结果；
4. `choice_agent` shadow 模式：生成内部候选和评分，但仍由旧 `options_service` 返回前台结果。

这个切片可以先验证：

- 数据是否完整保存；
- 上下文是否可控；
- 新选项通路是否优于旧通路；
- 不改变前端的前提下是否能安全接管。

第二个切片再接入事件图、目标层和局部修复；第三个切片才让 Agent 正式提交剧情结果。

---

## 三十四、最终工程判断

这套系统最适合的不是“一个模型负责全部事情”，而是：

```text
代码事实层
+ 原著世界模型
+ 长程事件图
+ 专用规划 Agent
+ 专用选项 Agent
+ 并行正文生成
+ 局部验证与修复
+ 供应商缓存
+ 不可压缩故事账本
```

模型调用数量可以增加到当前的数倍甚至十倍，但每个调用都必须拥有明确职责、明确输入预算、明确输出契约和明确失败处理。只有这样，增加 token 才会转化为更好的推演，而不是更长的等待、更大的上下文噪声和更多互相矛盾的结果。


# 三十五、完整施工方案（AI 可执行版）

本章是唯一施工主计划。后续实现以本章为准，前文概念设计作为背景参考。施工必须遵守“一个阶段一个交付物、一个功能点一个测试、一个验收门一个结论”。任何 AI 实现任务完成后都必须回填功能追踪表，不能只报告“代码已写”。

## 35.0 AI 实施工单格式

每个工单必须包含：工单编号、目标、前置工单、源码范围、禁止范围、实现步骤、数据契约、测试、验收、回滚、证据。AI 完成工单必须先读取源码和测试，只修改声明范围，补充测试，运行针对性测试和全量回归，保存输出，并明确剩余未实现项。

## 35.1 功能追踪表

| ID | 功能点 | 主要源码 | 必须证据 | 状态 |
|---|---|---|---|---|
| F01 | 完整故事账本 | `core/engine/story_ledger.py` | ledger 回归 | 待实施 |
| F02 | 全量小说导出 | `core/server.py`、`novel_exporter.py` | 10 回合导出 | 待实施 |
| F03 | 回合快照/hash | `story_snapshot.py` | hash/不可变测试 | 待实施 |
| F04 | 回合事务提交 | `turn_transaction.py` | 回滚/幂等 | 待实施 |
| F05 | 事件图/时序 | `story_graph.py` | 依赖测试 | 待实施 |
| F06 | 统一目标层 | `narrative_objective.py` | 许愿/任务测试 | 待实施 |
| F07 | 历史动态权重 | `story_weighting.py` | 衰减/关注测试 | 待实施 |
| F08 | 生成因素权重 | `story_weighting.py` | 分阶段权重 | 待实施 |
| F09 | 200k 上下文预算 | `story_context.py` | 超限/裁剪测试 | 待实施 |
| F10 | 三层长期记忆 | `context_compressor.py` | 100 回合保真 | 待实施 |
| F11 | 专用剧情 Agent | `story_agent.py` | shadow 集成 | 待实施 |
| F12 | 专用选项通路 | `choice_agent.py` | 分支质量 | 待实施 |
| F13 | 反事实模拟 | `counterfactual.py` | patch 预演 | 待实施 |
| F14 | 连续性审查 | `continuity_agent.py` | 不变量测试 | 待实施 |
| F15 | 连接鲁棒性 | `model_gateway.py` | 故障注入 | 待实施 |
| F16 | Anthropic 缓存 | `provider_cache.py` | 命中测试 | 待实施 |
| F17 | OpenAI 缓存 | `provider_cache.py` | 命中测试 | 待实施 |
| F18 | 导出连续性报告 | `export_continuity.py` | manifest 测试 | 待实施 |
| F19 | 前台兼容 | `server.py`、serializer | API 契约 | 待实施 |
| F20 | 灰度/回滚 | 配置/路由 | 模式切换 | 待实施 |

只有“实现、测试、验收、证据”四项都完成，功能点才能标记完成。

## 35.2 阶段 0：基线冻结

**实现**：保存源码/依赖快照，记录当前延迟、token、选项通过率、history、导出章节数；固定 5/30/100 回合回放；记录第 N 回合导出缺失前序回合的现状。

**验收**：基线可重复、旧测试可运行、密钥扫描通过、前台 API 未变。

**停止条件**：基线缺项、测试结果不可重现或发现未授权私有数据进入公开产物。

## 35.3 阶段 1：story ledger 与导出修复（F01/F02）

**实现顺序**：

1. 新增 ledger schema、幂等追加、合法性校验。
2. 在唯一 committed 提交点写入完整正文、选项、事件和 hash。
3. 为旧存档增加迁移，无法确定的回合标记 uncertain。
4. 修改 `extract_narrative()` 支持 ledger。
5. 修改 `_run_export()` 优先读取 ledger，history 仅作 fallback。
6. 增加 manifest 的起止 round、turn count、gaps、source kind。

**测试**：10 回合 history 压缩但 ledger 完整；第 10 回合导出 1–10；重复提交不重复；缺口被报告；导出失败不破坏存档。

**验收门**：`source_kind=story_ledger`、覆盖率 100%、全文非空、章节和 manifest 一致。

## 35.4 阶段 2：快照、事务、事件图、目标层（F03–F06）

**实现**：新增 `StorySnapshot`、`TurnTransaction`、事件依赖和时间窗口、`NarrativeObjective`；所有模型结果只能提交提案，代码在临时 state 上模拟并校验。

**测试**：前置事件未完成时拒绝后置事件；非法 patch 回滚；能力冷却、关系、物品和任务边界有效；许愿有激活/推进/兑现/受阻状态。

**验收门**：状态只提交一次；失败不改变 durable state；round 不重复；目标不静默丢失。

## 35.5 阶段 3：全文先验与动态权重（F07/F08）

**实现**：从原著生成 hard anchor、soft trajectory、branch opportunity、foreshadowing、spoiler guard；实现事件权重和生成因素权重；按战略、正文、选项、patch、润色阶段生成 effective weight。

**测试**：无玩家干预时轨迹接近先验；分叉点输入能改变局部路径；已解决事件降权但可回溯；玩家持续关注目标时权重上升；冲突因素被抑制。

**验收门**：每个高权重结论都有证据和原因；权重可复算；压缩后高权重事实仍可检索。

## 35.6 阶段 4：上下文预算与长期记忆（F09/F10）

**实现**：新增 `PromptLayers`、`ContextBudgetManager`；设置 P0–P3 优先级；根据 provider 上限、输出预算、安全余量计算可用输入；压缩时同步生成 fact/event/timeline/objective/character/evidence 索引。

**测试**：上下文不超过模型上限；P0 不裁剪；30/50/100 回合压缩后关键事实可检索；每个子任务使用独立预算。

**验收门**：战略层可接近 200k；段落/选项/裁判不强行携带全量上下文；context audit 完整。

## 35.7 阶段 5：连接鲁棒性和缓存（F15–F17）

**实现**：在统一模型网关中加入连接/首字节/整体超时、指数退避、错误分类、熔断、取消、幂等 request id 和 checkpoint；接入 provider-neutral cache，再实现 Anthropic/OpenAI 适配。

**测试**：超时、断线、429/5xx、截断 JSON、部分并发失败、缓存不可用、备用 provider 不可用。

**验收门**：hard deadline 内回滚或降级；不重复推进 round；静态前缀可命中；跨作品和版本不串缓存；无 key 泄露。

## 35.8 阶段 6：专用剧情 Agent（F11/F14）

**实现顺序**：

1. `StoryAgent` 读取 snapshot。
2. Wave 0 本地检索和缓存。
3. Wave 1 并发 3–6 个战略候选。
4. 代码过滤后由裁判选择 TurnPlan。
5. 段落、角色反应、选项内部候选并发。
6. 运行状态、时序、因果、角色、目标和质量检查。
7. 局部修复。
8. 通过事务提交。

**测试**：shadow 模式同时运行旧流程和 Agent；比较计划、正文、选项、patch、token、缓存和延迟；验证 legacy 可回退。

**验收门**：新旧前台契约一致；Agent 不直接写状态；事务只提交一次；100 回合 ledger 覆盖率 100%。

## 35.9 阶段 7：专用选项通路（F12/F13）

**实现顺序**：

1. 读取目标、事件、角色、资源和承诺锁。
2. 生成 8–16 个内部行动候选。
3. 代码做前置条件和 patch 过滤。
4. 前 8 个执行 1–3 回合反事实模拟。
5. 做风险、代价、目标和多样性评分。
6. 集合级最大覆盖选择 6 项。
7. 根据正文证据生成前台文案。
8. 只重填失败 slot。

**测试**：选项可执行、正文有依据、六项不重复、至少包含主线/关系/信息/风险/性格覆盖；选择后承诺锁在下一回合生效。

**验收门**：对外仍是 A-F、text、preview、factor；内部候选不泄露；选项质量不低于基线且关键指标提升；等待时间在 hard deadline 内。

## 35.10 阶段 8：小说导出升级（F18）

**实现**：ledger 校验 → 全书目录/时间线 → 章节并行改写 → 跨章连续性审查 → 局部修订 → assemble → 报告。

**测试**：第 1–N 回合覆盖；章节顺序正确；人物、称谓、伤势、物品、能力、时间、地点一致；无游戏痕迹；模型失败时 faithful fallback 可用。

**验收门**：全文回合覆盖、章节非空、manifest 一致、事实不变、导出失败不影响游戏存档。

## 35.11 阶段 9：前台兼容和灰度切换（F19/F20）

**实现**：保留现有 API 和前端字段；增加 `legacy/shadow/agent`；保留旧 options、旧导出 fallback 和紧急配置。

**测试**：旧客户端、A-F、自由输入、存档/读档、强化/基础/碎锚路径全部运行；模式切换后状态和 API 一致。

**验收门**：连续 30 回合无状态破坏；100 回合导出完整；P95 延迟达标；随时可切回 legacy。

## 35.12 最终全量验收流程

按以下顺序执行，不得只运行单元测试：

1. 静态检查、类型检查、密钥扫描。
2. 全量现有测试。
3. 新增模块单元测试。
4. API 契约和前端黑盒测试。
5. 故障注入测试。
6. 5 回合快速烟测。
7. 30 回合质量实验。
8. 50 回合长程实验。
9. 100 回合长程实验。
10. save/load/export 全链路测试。
11. Anthropic/OpenAI 缓存命中测试。
12. shadow 差异报告。
13. 性能报告和 token 报告。
14. 安全扫描和发布包扫描。

## 35.13 最终通过标准

只有同时满足以下条件，才允许标记“施工完成”：

- 所有 F01–F20 均有实现、测试、验收和证据；
- 前台 API 与 A-F 交互没有破坏；
- 第 N 回合导出覆盖 1–N 全部 committed 回合；
- 100 回合状态、事件、目标和 ledger 一致；
- 事件和状态硬错误为 0；
- 选项集合质量不低于基线；
- 单次请求不超过 provider 上限，战略层可稳定接近 200k；
- hard deadline 生效，连接故障可恢复或回滚；
- Anthropic/OpenAI 缓存可命中且隔离正确；
- 私有数据、密钥和完整敏感 prompt 不进入日志和公开包；
- legacy 回退可用；
- 所有报告和测试产物已保存。

## 35.14 AI 实现时的防遗漏规则

每次修改前后都必须执行功能点反查：

```text
数据结构是否落地？
调用入口是否接通？
成功路径是否接通？
失败路径是否接通？
持久化是否接通？
读取/恢复是否接通？
前台兼容是否验证？
测试是否真实运行？
报告是否保存？
回滚是否可用？
```

任何一项答案为“没有”，该功能只能标记为“部分实现”，不得标记完成。


## 三十五点十五、当前实际施工进度

截至本次施工，已真实完成阶段 0 与阶段 1 的可运行部分：

- 阶段 0 基线测试已保存至 `artifacts/story_agent/phase-0/`；
- 新增 `core/engine/story_ledger.py`；
- 初始 state 增加 `story_ledger`；
- `story_ledger` 纳入回合事务回滚键；
- committed 回合在最终提交处幂等追加；
- 导出优先读取 committed ledger；
- 导出 manifest 写入 source kind、回合范围、回合数量和 gap 字段；
- 旧存档恢复时缺失 ledger 自动补为空列表；
- 新增 5 项 ledger 专项测试；
- 全量测试 413 项通过。

尚未声称完成的部分：

- StoryAgent 正式主流程；
- 200k ContextBudgetManager；
- 动态历史/因素权重；
- choice agent 反事实选项通路；
- Anthropic/OpenAI provider cache；
- checkpoint、熔断和多 provider 自动降级；
- 全书连续性导出 Agent。

后续阶段必须在本阶段证据保留的前提下继续，不能用设计文档代替实装和测试。

## 三十五点十六、剩余施工阶段实装与验收记录

本轮继续完成并测试了以下基础能力：

- `core/engine/story_snapshot.py`：状态快照与稳定 hash；
- `core/engine/story_graph.py`：事件前置和时间检查；
- `core/engine/narrative_objective.py`：许愿/任务等目标归一化；
- `core/engine/story_weighting.py`：历史事件和生成因素动态赋权；
- `core/services/story_context.py`：分层上下文预算与裁剪；
- `core/services/provider_cache.py`：provider-neutral 稳定前缀和缓存键；
- `core/services/story_agent.py`：shadow 准备编排；
- `core/services/choice_agent.py`：候选过滤、多样性选择和前台 A-F 映射；
- `core/engine/story_invariants.py`：回合和选项硬约束；
- `core/engine/export_continuity.py`：导出回合缺口/重复/空正文报告；
- `_run_export()` 已返回 continuity 报告。

本轮专项测试 14 项、全量测试 422 项均通过，证据位于 `artifacts/story_agent/phase-2-final/`。

必须明确的未完成真实验收项：

1. 尚未使用真实 Anthropic/OpenAI 凭据证明供应商端 cache hit；当前完成的是安全的 provider-neutral 缓存键/稳定前缀适配。
2. 尚未进行真实 30/50/100 回合模型质量实验，因此不能声称长程质量指标已达标。
3. 连接断线、429/5xx 和多 provider failover 的真实网络故障注入尚未完成；当前仅完成接口基础模块。
4. StoryAgent 当前为 shadow preparation，不应被描述为已经正式接管主回合提交。

旧 legacy 路径保留，可作为后续正式 Agent 接管前的回滚路径。


## 三十五点十七、真实长剧情实验前工程施工完成记录

本阶段已完成并通过离线测试的工程内容：

- `turn_transaction.py`：回合事务状态机、临时状态应用和回滚；
- `counterfactual.py`：选项临时状态反事实模拟；
- `story_invariants.py`：回合、选项和正文证据基础校验；
- `resilient_gateway.py`：有界重试与熔断基础；
- `StoryAgent` shadow preparation 已接入强化模式元数据；
- `choice_agent` 候选过滤、多样性选择和 A-F 兼容映射；
- `export_continuity` 已接入导出结果；
- story ledger 与 ledger-first export 已接入。

本阶段测试：专项 19 项通过，全量 427 项通过。

### 当前工程边界

在启动真实 30/50/100 回合实验前，以下工程骨架已齐全：数据账本、状态快照、事务回滚、事件基础检查、目标归一化、动态权重、上下文预算、反事实、选项特殊通路基础、连续性报告、缓存键、重试熔断基础和 shadow 元数据。

仍必须在真实实验阶段使用真实 provider 验证的项目：

1. Anthropic prompt caching 的真实 cache creation/read 命中；
2. OpenAI prompt caching 的真实 cached input 命中；
3. 真实 provider 的 200k 上下文计数与截断边界；
4. 真实 30/50/100 回合剧情质量、延迟和 token 指标；
5. 真实断线、429、5xx 和多 provider failover；
6. StoryAgent 从 shadow 正式切换为 agent 主流程后的长程质量；
7. choice_agent 在真实生成中替换现有选项主通路后的质量提升。

这些项目属于真实模型运行和外部服务验收，不能用离线 FakeModel 测试代替。

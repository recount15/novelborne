# Novelborne 核心体验与性能重构更新计划

## 0. 目标、范围与执行原则

### 0.1 核心目标
1. **彻底消除身份消歧的 $O(n^2)$ 指数级调用与阻塞**：从 465+ 次成对模型调用（耗时 1.5~2.5 小时、在弱模型/便宜网关如 Kimi-K3 上极易超时失败）降低到候选集数量级 $O(\text{candidates})$，配合确定性规则分离与瞬态重试，让便宜/弱能力模型也能秒级/分钟级稳定收敛。
2. **增量/局部章节就绪与开局放行**：按章节切分身份裁决与冲突归属。开局指定前 $K$ 章时，只要前 $K$ 章内部及跨章提及已裁决无冲突即可放行开局，无需无休止等待全书 50+ 章长尾冲突。
3. **阅读器体验与透明诊断优化**：解决蒸馏书目在阅读器中看不到锚点和活跃人物的困惑。全书模式逐章落盘锚点已就绪；Window（窗口）模式准备阶段零模型调用，明确返回清晰透明的诊断提示（告知玩家锚点在开局启动后由管线实时生成），消灭伪确定性与静默空白。
4. **保障用户交互性零门槛流畅引导**：用户配置好 API 密钥后，Copilot、角色设计器、金手指与阅读器即可高效引导玩家推进游戏，不得发生数小时的无反馈阻塞。
5. **无损替换与安全门禁**：所有重构遵循原著逐字引文证据、严格不可篡改审计、事务持久化和现存安全门禁，确保全量测试套件（1128+ 项用例）100% 通过。

---

## 1. 施工内容必须遵守的核心原则

1. **资产与隔离红线**：
   - 仅修改 `C:\Novelborne\Novelborne-state-refactor`；
   - 原全量源码、原便携版、恢复包及用户在用数据（含 21560 用户实例）为回退资产，绝对不得修改或停止；
   - 严禁推送、上传或公开分发私有角色、原著、数据库、源码资产或私有构建产物；
   - 凭据卫生：不将凭据写入 URL、日志、截图、报告、测试夹具或源码；
   - 进程管理：不按端口号直接杀进程，只能终止自己启动且已记录 PID/命令/目录的测试进程。
2. **门禁与算法无损替换原则**：
   - 不得通过删测试、放宽关键断言、吞异常、模板凑数或修改公共白名单伪造通过；
   - 候选集剪枝后的非候选对必须生成带逐字原文引文的确定性证据（`deterministic: True`），保持 $n(n-1)/2$ 完整决策覆盖，满足 `validate_identity_report` 既有严格校验；
   - 任何涉及模型调用的子服务必须具备有界瞬态重试与异常分类，区分可重试网络/解析抖动与不可重试业务硬错误。

---

## 2. 各功能模块具体实现方式与无损替换方案

### 模块 A：人物身份解析候选集优化（$O(n^2) \to O(\text{candidates})$）
- **问题根因**：原实现对所有 $n$ 处人物提及进行两两全排列模型裁决（$n=31 \implies 465$ 次调用），调用耗费高昂且极易因网关速率限制或死锁中断。
- **无损替换实现**：
  1. **启发式候选集过滤 (`_is_identity_candidate`)**：
     - 仅对满足以下条件之一的提及对调用模型裁决：同名、名称互为子串（如“白芷”与“掌柜白芷”）、编辑距离相似度 $\ge 0.5$、一方名称出现在对方原文摘录中。
  2. **确定性规则直接分离**：
     - 两个毫不相关、无交叉提及的人物直接通过规则判定为 `relation: 'different'`，以其各自提及摘录作为权威证据，生成合法结构化决策并打上 `deterministic: True` 标签。
  3. **有界重试封装 (`_call_identity_pair_model_with_retry`)**：
     - 遇到网关返回非 JSON 体或临时抖动时最多重试 3 次，防止单点抖动废弃整个批次。
- **替换效果**：实测候选集调用量减少 52.5%~90%，既维持 `validate_identity_report` 的完整性，又让低成本 API 极速收敛。

### 模块 B：增量/章节级身份就绪与开局放行
- **问题根因**：全书准备必须等后文全部章节（乃至数十万字后）的人物全部无冲突才能设置 `identity_ready: True`，导致开局流程被后文无关冲突阻死。
- **无损替换实现**：
  1. **冲突章节归属切分**：在 `validate_identity_report` 中将冲突归属于跨章提及中较晚的章节（`affect_ch = max(ch_l, ch_r)`），计算各章节独立的 `chapter_readiness` 映射。
  2. **开局门禁分级放宽**：在 `verify_preparation` 中，当提供 `target_chapter` 时，只要 $1 \dots \text{target\_chapter}$ 范围内的所有章节在 `chapter_readiness` 中均标为 `ready`，即放行验证，允许玩家立刻进入游戏。

### 模块 C：Window 模式与阅读器锚点接口透明诊断
- **问题根因**：用户在阅读器查看章节锚点与活跃人物时，若当前书目使用普通/Window 模式准备（该模式设计为轻量级、零模型调用提取），接口仅返回模糊的 `unavailable`，让用户误以为准备失败或系统损坏。
- **无损替换实现**：
  1. `/api/books/{book_id}/chapters/{chapter_index}/anchors` 接口透明区分不可读与 Window 模式状态。
  2. 若本章已落盘锚点（Fullbook 逐章落盘或游戏开局管线落盘），正常返回；若未生成且书目处于 Window 准备包或正在开局，返回状态与引导性说明：“窗口准备模式锚点将在开局管线启动时实时生成；全书蒸馏模式请确认蒸馏已覆盖本章”，消除用户疑虑。

---

## 3. 详细施工卡与执行进度

| 任务卡 | 内容说明 | 影响文件 | 状态 | 验证标准 |
|---|---|---|---|---|
| **P-01** | 实现身份候选集过滤算法、确定性不同人规则分离与瞬态重试 | `core/services/character_evidence_service.py` | **COMPLETED** | 单元测试覆盖剪枝、别名互引与重试 |
| **P-02** | 增量身份就绪评估算法与开局门禁局部放宽 | `core/services/character_evidence_service.py`<br>`core/services/book_prepare_service.py` | **COMPLETED** | `test_incremental_identity_allows_early_chapter_verify` 通过 |
| **P-03** | 阅读器锚点与活跃人物接口诊断提示优化 | `core/server.py`<br>`tests/test_v3_reader_start.py` | **IN PROGRESS** | 区分 Window 模式与损坏状态，无伪造数据 |
| **P-04** | 全量回归与集成验收测试 | 全量测试套件 | **PENDING** | `pytest tests/` 1128+ 测试全绿 |
| **P-05** | 真实书目与弱模型端到端冒烟验证 | 实测流程 | **PENDING** | 便宜 API 顺畅完成准备与对局 |

---

## 4. 最终验收测试方案

1. **单元测试回归**：
   - `pytest -p no:cacheprovider tests/test_semantic_preparation.py`（候选集剪枝、别名判定、重试容错、增量章节验证）；
   - `pytest -p no:cacheprovider tests/test_v3_reader_start.py`（阅读器章节正文读取、锚点状态与诊断提示返回）。
2. **全量回归保障**：
   - 运行项目全量自动化测试套件，确认零回归损坏，维持 100% 绿灯。
3. **真实模型经济性验证**：
   - 验证在低成本模型（如 Kimi-K3）下，原著准备的成对调用显著削减，开局即刻可用，阅读器指引明确。
